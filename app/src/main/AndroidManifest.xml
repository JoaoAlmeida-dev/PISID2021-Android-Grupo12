<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pisid2021">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:debuggable="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".app.activities.PrincipalActivity"></activity>
        <activity android:name=".app.activities.AlterarParametroActivity" />
        <activity android:name=".app.activities.VerCulturaActivity" />
        <activity
            android:name=".app.activities.DatePickerActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".app.activities.AlertasActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".app.activities.MedicoesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".app.activities.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>